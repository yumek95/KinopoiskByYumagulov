<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/favorites"
        android:title="@string/favorites"
        android:icon="@drawable/ic_round_favorites"
        app:showAsAction="withText"/>

    <item
        android:id="@+id/watch_later"
        android:title="@string/watch_later"
        android:icon="@drawable/ic_round_watch_later"
        app:showAsAction="withText"/>

    <item
        android:id="@+id/compilations"
        android:title="@string/compilations"
        android:icon="@drawable/ic_round_compilations"
        app:showAsAction="withText"/>

</menu>